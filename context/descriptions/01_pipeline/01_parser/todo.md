# Stage 01 — Parser: open work

No parser-stage work is open.

`norm()` and DECIDE-variable `IN (...)` retain their parsed syntax through
binding, then become optimizer-owned mathematical formulations. The parser still
owns source structure, scoped-variable spelling, and checks that must observe the
user-written tree (notably explicit decision casts).
