# Query Diagnostics — Infeasible (how it works)

> Router terminal: **failed → infeasible** (`elastic` → report). See `router/README.md`.

Nothing implemented yet — see `todo.md`. Current behavior: both backends throw a
static error paragraph on `INFEASIBLE` (`gurobi_solver.cpp`,
`deterministic_naive.cpp`).
