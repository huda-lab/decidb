# Query Diagnostics — Infeasible (implemented)

Nothing implemented yet — see `todo.md`. Current behavior: both backends throw a
static error paragraph on `INFEASIBLE` (`gurobi_solver.cpp:229-236`,
`deterministic_naive.cpp:208-216`).
