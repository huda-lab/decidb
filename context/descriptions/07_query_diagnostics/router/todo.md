# Router — todo

**All router work has shipped** — see `done.md` for how the classifier, the inf/unb
check-ray routing, and the unbounded and infeasible (elastic) terminals work, plus the
decision-tree unit tests. The time_limit terminal was removed in batch H: a slow solve is
execution behaviour, not a diagnosis.


